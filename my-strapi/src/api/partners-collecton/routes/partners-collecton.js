'use strict';

/**
 * partners-collecton router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::partners-collecton.partners-collecton');
