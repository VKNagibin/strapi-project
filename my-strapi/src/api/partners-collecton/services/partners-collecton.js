'use strict';

/**
 * partners-collecton service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::partners-collecton.partners-collecton');
